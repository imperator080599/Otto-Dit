{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "regions": ["cdg1"],
  "installCommand": "npm install --no-audit --no-fund && cd app && npm install",
  "buildCommand": "cd app && npm run deploy:reconstruire && npx next build",
  "outputDirectory": "app/.next"
}
