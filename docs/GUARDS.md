# Le registre des gardes

*Généré depuis `app/src/lib/gardes/registre.ts` par `npm run gardes -- --figer` ; `npm run gardes`
échoue si ce fichier a divergé du registre. Ne pas éditer à la main.*

**Comment lire « prouvée ».** Une garde SQL est prouvée par `npx vitest run src/lib/gardes` en DEUX
passes : son attaque est jouée normalement (elle doit être refusée, par ELLE — le refus est
comparé à son expression), puis dans une transaction annulée où la garde est neutralisée (elle
doit réussir). Si elle refuse encore, l’attaque n’a jamais atteint la garde et le test ÉCHOUE en la
nommant. Une garde de SERVICE n’a qu’une passe (aucune neutralisation SQL) : son refus nommé. Une
garde DÉCLARÉE n’est pas prouvée par ce registre : la colonne dit où vit sa preuve, ou qu’il n’y en a
pas. L’épreuve elle-même est éprouvée contre trois gardes connues mauvaises (règle 17).

**Compte** : 26 garde(s) SQL à deux passes · 6 garde(s) de service à une passe · 21 déclarée(s), dont 1 sans aucune preuve. **Verdicts observés** (docs/GARDES_RESULTATS.json, écrit par `npx vitest run src/lib/gardes`) : 32 prouvée(s), 0 sans résultat.

**Retirer une garde, et voir le registre le dire** : le test « une garde RETIRÉE est dénoncée par son nom » (gardes.test.ts) désactive `review_note_close_guard` et lit le verdict « G-03 : l’attaque a RÉUSSI sans neutralisation — la garde n’existe pas » ; c’est le critère du plan, joué à chaque exécution.

## Gardes SQL — prouvées en deux passes

| Code | Verdict observé | Invariant | Tenu par | Rayon si elle tombe | Refus attendu | Neutralisation d’épreuve | Où elle cesse de regarder |
|---|---|---|---|---|---|---|---|
| G-01 | PROUVÉE (2026-09-02) | Le journal des événements est en ajout seul : ni mise à jour, ni suppression. | `déclencheur event_log_append_only (0003) → forbid_mutation()` | La piste d’audit entière : une ligne réécrite est une histoire réécrite. | `append-only` | `alter table event_log disable trigger event_log_append_only` | Ne regarde pas le contenu des lignes ajoutées ni la continuité de la chaîne de hachage (vérifiée par events.test.ts). |
| G-02 | PROUVÉE (2026-09-02) | Les visas se posent dans l’ordre préparateur → réviseur → associé. | `déclencheur signoff_order_guard (0009) → assert_signoff_order()` | Un visa d’associé posé sans revue : la hiérarchie de revue n’existe plus. | `review order` | `alter table signoff disable trigger signoff_order_guard` | Ne regarde pas QUI pose le visa (le droit de signature est vérifié par le service, can_sign), seulement l’ordre des rôles. |
| G-03 | PROUVÉE (2026-09-22) | L’auteur d’une note de revue ne la clôt jamais ; seul un réviseur de la mission la clôt. | `déclencheur review_note_close_guard (0024) → assert_note_close_by_reviewer()` | Une revue vidée de sa substance : chacun ferme ses propres notes. | `auteur d.une note ne la clôt jamais` | `alter table review_note disable trigger review_note_close_guard` | Ne regarde pas le contenu de la réponse ni si la note visait un objet encore existant. |
| G-04 | PROUVÉE (2026-09-22) | Une note close ne se rouvre pas — on en pose une nouvelle. | `déclencheur review_note_close_guard (0024), seconde branche` | L’historique d’une note réécrit : ce qui a été clos ne l’a plus été. | `ne se rouvre pas` | `alter table review_note disable trigger review_note_close_guard` | Ne regarde pas les réponses ajoutées après clôture (review_note_reply a sa propre garde de verrou). |
| G-05 | PROUVÉE (2026-09-02) | Un écart ne se résout qu’avec explication, réponse du client, disposition, pièce corroborante, auteur et date — chiffré ou non. | `contrainte exception_resolution_is_probative (0009)` | Des écarts « résolus » par une phrase générique — et une anomalie de 36 800 € qui disparaît du total des anomalies connues (FA2025-0702) : la disposition du montant est exigée ICI ; la contrainte « chiffré ⇒ disposition » de 0009 était inerte puis recouverte, retirée en 0037. | `exception_resolution_is_probative` | `alter table exception drop constraint exception_resolution_is_probative` | Ne juge pas la QUALITÉ de l’explication ni la pertinence de la pièce désignée ; seulement leur présence. |
| G-07 | PROUVÉE (2026-09-02) | Surcharger un niveau de risque par assertion exige un motif écrit, un décideur et une date. | `contrainte override_needs_a_written_reason (0012)` | Un niveau abaissé sans trace : moins de travaux, et personne ne sait pourquoi (ADR-094). | `override_needs_a_written_reason` | `alter table fsli_assertion_risk drop constraint override_needs_a_written_reason` | Ne juge pas le motif ; ne vérifie pas que le décideur avait le rôle pour décider. |
| G-08 | PROUVÉE (2026-09-02) | Répondre « une information produite par l’entité a été utilisée » sans désigner un rapport IPE documenté est refusé. | `contrainte ipe_documente (0031, forme de 0036 : utilisee = false ou rapport_id posé)` | Un test substantif tiré d’un listing jamais éprouvé — l’insuffisance d’inspection la plus fréquente. | `ipe_documente` | `alter table ipe drop constraint ipe_documente` | La documentation (nature, pièce, exhaustivité, exactitude) est celle du rapport (G-12) ; l’arrêté attendu et la pertinence pour CE test sont exigés par le service (utiliserRapport), pas par la base. |
| G-09 | PROUVÉE (2026-09-22) | Un dossier scellé n’accepte plus d’écriture hors du flux d’amendement justifié. | `déclencheurs <table>_lock_guard (0003) → assert_engagement_unlocked()` | Un dossier archivé modifié après coup : le scellé ne scelle rien. | `writes rejected` | `alter table review_note disable trigger review_note_lock_guard` | Ne couvre que les tables portant engagement_id EN DIRECT, listées en 0003/0021/0022/0023/0177 — celles qui l’atteignent par jointure (0177, assert_engagement_unlocked_via, G-32/G-33) sont éprouvées à part. Une table nouvelle sans verdict au registre est vue par couverture-verrou.test.ts (colonne engagement_id directe) ; une table fille future ne l’est par AUCUN instrument générique (règle 19). |
| G-10 | PROUVÉE (2026-09-02) | Accepter ou refuser une mission sans motif écrit est impossible. | `contrainte decision_needs_a_written_reason (0017)` | La pièce qu’un inspecteur demande en premier quand un dossier tourne mal — absente. | `decision_needs_a_written_reason` | `alter table engagement_acceptance drop constraint decision_needs_a_written_reason` | Ne relit pas le questionnaire d’acceptation ; la cohérence des réponses est celle du service. |
| G-11 | PROUVÉE (2026-09-02) | Une déclaration d’indépendance est signée par son propre auteur — personne ne signe pour un autre. | `contrainte declaration_signed_by_self (0011)` | Une indépendance déclarée par un autre : le fondement même de l’attribution des travaux. | `declaration_signed_by_self` | `alter table independence_declaration drop constraint declaration_signed_by_self` | Ne sait pas si les réponses sont vraies ; ne vérifie que QUI signe. L’intégralité de la signature (declaration_signature_is_whole) et le motif de révision (declaration_revision_has_a_reason) ne sont pas au registre — reportés. |
| G-12 | PROUVÉE (2026-09-02) | Un rapport IPE désigne exactement un fichier du dossier et documente ses deux éléments testés. | `contrainte ipe_rapport_documente (0036)` | Un état système « éprouvé » sans exhaustivité ni exactitude — partagé par tous les papiers qui le désignent. | `ipe_rapport_documente` | `alter table ipe_rapport drop constraint ipe_rapport_documente` | Ne vérifie pas que l’arrêté déclaré est celui du fichier (c’est le service qui refuse la réutilisation sur un autre arrêté). |
| G-13 | PROUVÉE (2026-09-02) | TEST-01 — une cellule de test « conforme » porte une ancre : pièce, page, rectangle. | `contrainte test_cell_green_needs_anchor (0050)` | Une cellule verte que la pièce ne montre nulle part — un vert qu’on croit sur parole. | `test_cell_green_needs_anchor` | `alter table test_cell drop constraint test_cell_green_needs_anchor` | Ne vérifie pas que le rectangle est AU BON ENDROIT de la pièce : c’est la lecture de la couche texte (ancres.ts) qui le place, et l’œil qui le contrôle. |
| G-14 | PROUVÉE (2026-09-02) | TEST-02 — une ligne dont un attribut d’identité diverge (tiers, numéro de pièce) ne se conclut pas : la preuve n’est pas recevable. | `déclencheur test_line_conclusion_1_identity (0050)` | Une ligne conclue sur la facture d’un AUTRE client — le chiffre d’affaires « justifié » par la mauvaise pièce. | `TEST-02` | `alter table test_line_conclusion disable trigger test_line_conclusion_1_identity` | Ne juge pas la ressemblance des noms : c’est le calcul de la cellule (normalizeParty) qui décide « diverge » ; le déclencheur ne lit que l’état. |
| G-15 | PROUVÉE (2026-09-02) | TEST-03 — une disposition de cellule porte un motif écrit. | `contrainte cell_disposition_has_reason (0050)` | Une cellule hors tolérance « disposée » par un clic, sans un mot — la décision humaine vidée de son contenu. | `cell_disposition_has_reason` | `alter table cell_disposition drop constraint cell_disposition_has_reason` | Ne juge pas la qualité du motif : « ok » passe. Le motif est lu par le reviewer, pas par la base. |
| G-16 | PROUVÉE (2026-09-02) | TEST-04 — une ligne dont une cellule est hors tolérance, absente ou sans ancre ne se conclut pas sans disposition écrite. | `déclencheur test_line_conclusion_2_cells (0050)` | Une ligne conclue « conforme » avec un écart de montant que personne n’a regardé. | `TEST-04` | `alter table test_line_conclusion disable trigger test_line_conclusion_2_cells` | Ne relit pas la cellule après la conclusion : une cellule recalculée depuis rend la conclusion PÉRIMÉE (empreinte des cellules), c’est l’écran qui le dit, pas ce déclencheur. |
| G-17 | PROUVÉE (2026-09-02) | ANA-01 — une revue analytique vide n’est pas une revue analytique. | `contrainte fsli_analytique_text_not_empty (0130)` | Un poste « revu » par un enregistrement à blanc — la section existe, elle ne dit rien, et le statut la compte. | `fsli_analytique_text_not_empty` | `alter table fsli_analytique drop constraint fsli_analytique_text_not_empty` | Ne juge pas le contenu : « RAS » passe. La substance est lue par le réviseur, pas par la base. |
| G-18 | PROUVÉE (2026-09-02) | ANA-02 — une rédaction « proposée puis validée » cite le run qui l’a produite. | `contrainte fsli_analytique_proposal_has_run (0130)` | Une phrase attribuée au moteur sans la trace de son calcul : « d’où vient cette rédaction ? » sans réponse (P7). | `fsli_analytique_proposal_has_run` | `alter table fsli_analytique drop constraint fsli_analytique_proposal_has_run` | Ne vérifie pas que le run cité est bien celui de CE poste : la clé étrangère garantit qu’il existe, pas qu’il correspond. |
| G-19 | PROUVÉE (2026-09-02) | ANA-03 — une version de revue analytique ne se modifie ni ne s’efface : on en écrit une nouvelle. | `déclencheur fsli_analytique_append_only (0130)` | Un texte réécrit sous la même version, sans trace : l’inspecteur lit une rédaction qui n’est pas celle qui a été visée. | `ANA-03` | `alter table fsli_analytique disable trigger fsli_analytique_append_only` | Ne protège pas contre une suppression en cascade par la table mère : engagement est en « on delete restrict », donc rien ne cascade — mais c’est cette autre garde qui le tient. |
| G-22 | PROUVÉE (2026-09-03) | REPLI-01 — une clé de repli hors format est refusée : lettres, chiffres, « . _ : - », 120 caractères au plus. | `contrainte ui_repli_cle_valide (0132) ; le service memoriserRepli lit le même prédicat avant la base` | Une clé libre est un canal d’écriture arbitraire ouvert à tout compte connecté : n’importe quel texte, sans borne, mémorisé sous le nom de la personne et relu par chaque écran. | `ui_repli_cle_valide` | `alter table ui_repli drop constraint ui_repli_cle_valide` | Ne juge pas le SENS de la clé : une clé bien formée qui ne correspond à aucune section est mémorisée et jamais lue. Ne borne pas le NOMBRE de clés par personne. |
| G-28 | PROUVÉE (2026-09-23) | VISA-01 — un même rôle ne vise pas deux fois le même papier. | `déclencheur visa01_unicite (0176) → assert_visa01_unicite()` | Un second visa preparer_validator sur le même papier : le compte de visas ne dit plus qui a réellement revu quoi. | `VISA-01` | `alter table signoff disable trigger visa01_unicite` | Ne regarde pas QUI vise, ni si la personne a le droit de tenir ce rôle (VISA-03 le tient) — seulement l’UNICITÉ du rôle sur ce papier. |
| G-29 | PROUVÉE (2026-09-23) | VISA-02 — la même personne ne vise pas deux rôles sur le même papier. | `déclencheur visa02_utilisateur_distinct (0176) → assert_visa02_utilisateur_distinct()` | Un préparateur qui se revoit lui-même : la séparation des rôles du visa devient un décor. | `VISA-02` | `alter table signoff disable trigger visa02_utilisateur_distinct` | Ne regarde pas L’ORDRE des rôles (signoff_order_guard, 0009, le tient) ni le DROIT au rôle (VISA-03) — seulement que la personne diffère d’un rôle à l’autre. |
| G-30 | PROUVÉE (2026-09-23) | VISA-03 — un visa reviewer exige un rôle manager ou partner ; un visa partner exige le droit de signature. | `déclencheur visa03_role (0176) → assert_visa03_role()` | Un préparateur (staff/senior) qui se donne un visa reviewer : la hiérarchie de revue n’est plus tenue par personne. | `VISA-03` | `alter table signoff disable trigger visa03_role` | Ne regarde pas SI la personne est membre de LA mission par ailleurs (assertMembre, côté service, le fait) — seulement son eng_role/can_sign une fois membre trouvé. |
| G-31 | PROUVÉE (2026-09-23) | EQUIPE-01 — le rôle ou le droit de signature d’un membre ne change que sous un acteur manager ou partner déclaré (otto.acteur_role). | `déclencheur equipe01_acteur_manager_partner (0176) → assert_equipe01_acteur()` | Un update direct de eng_role/can_sign, hors withActeur() : n’importe quel chemin d’écriture pourrait promouvoir un membre sans qu’aucun manager ni partner n’ait agi. | `EQUIPE-01` | `alter table engagement_member disable trigger equipe01_acteur_manager_partner` | Ne vérifie pas que la VALEUR posée dans otto.acteur_role est honnête — un appelant qui mentirait sur son propre rôle contournerait ce garde (limite nommée dans lib/db/acteur.ts). Ne regarde pas non plus l’INSERT (première affectation) CÔTÉ BASE — seul l’UPDATE d’un membre déjà affecté est gardé par CE déclencheur (04 §1 littéral). L’INSERT est gardé ailleurs, CÔTÉ SERVICE (team.ts::assignMember : manager/partner exigé, sauf la toute première affectation d’un dossier encore sans équipe, posée par la personne elle-même) — trouvé absent puis fermé par la revue hostile de P1-04 ; cette garde-ci ne l’exerce donc pas, G-31 ne teste que le déclencheur SQL. |
| G-32 | PROUVÉE (2026-09-23) | VERROU (fille, deux jointures) — un dossier scellé refuse un nouveau rapprochement de ligne d’échantillon, même si la table ne porte pas engagement_id en direct. | `déclencheur match_lock_guard (0177) → assert_engagement_unlocked_via('sample_item_id', … sample_item → sample → engagement)` | La preuve la plus complexe du mécanisme générique (0177) : si LA RÉSOLUTION à deux sauts se trompe (mauvaise colonne, mauvaise jointure), cette garde échoue à neutraliser OU à refuser — et aucune des 16 tables filles n’est plus fiable que celle-ci ne le prouve. | `writes rejected` | `alter table match disable trigger match_lock_guard` | N’éprouve QUE match — attribute_result partage la même résolution (sample_item_id) mais n’est pas rejouée ici séparément ; les 14 autres tables filles ne sont vérifiées que par leur PRÉSENCE (couverture-verrou.test.ts), jamais par leur REFUS (règle 17, limite assumée pour tenir la revue adverse proportionnée — règle 30). |
| G-33 | PROUVÉE (2026-09-23) | VERROU (fille, un saut) — un dossier scellé refuse un NOUVEAU visa, pas seulement sa modification. | `déclencheur signoff_lock_guard (0177) → assert_engagement_unlocked_via('workpaper_id', … workpaper → engagement)` | signoff est append-only (0003, forbid_mutation) : UPDATE et DELETE étaient déjà fermés, mais RIEN ne fermait l’INSERT avant 0177 — un dossier scellé pouvait recevoir un visa neuf. L’ajout-seul et le verrou sont deux questions distinctes, et seule celle-ci tient la seconde. | `writes rejected` | `alter table signoff disable trigger signoff_lock_guard` | Ne regarde pas VISA-01/02/03 (G-28..30, garde différente, même table) — seulement que le verrou de dossier referme l’INSERT que forbid_mutation ne fermait pas. |
| G-27 | PROUVÉE (2026-09-03) | Une ligne sortie du tirage ne se statue pas sans motif écrit, ni sans dire qui et quand. | `contrainte sample_item_sortie_statuee (0142) — les quatre colonnes vont ensemble, ou aucune` | La règle du re-tirage (ADR-133) repose sur une décision RELISIBLE. Une décision sans motif, ou sans auteur, laisse un inspecteur devant une ligne de travail abandonnée sans savoir par qui ni pourquoi — et le service TIRAGE-03 seul ne protège pas d’une écriture directe. | `sample_item_sortie_statuee` | `alter table sample_item drop constraint sample_item_sortie_statuee` | Ne regarde pas le CONTENU du motif (une phrase vide de sens passe), ni si la décision est pertinente — elle vérifie qu’il y en a une, écrite, signée et datée. Le catalogue des décisions possibles est tenu par un `check` séparé, dont le refus n’est pas nommé TIRAGE-0x : c’est une dette écrite en R32. |

## Gardes de service — prouvées en une passe

| Code | Verdict observé | Invariant | Tenu par | Rayon si elle tombe | Refus attendu | Où elle cesse de regarder |
|---|---|---|---|---|---|---|
| G-23 | PROUVÉE (2026-09-03) | REPLI-03 — le locataire d’un rangement vient de la PERSONNE : aucune écriture ne peut le poser au nom d’un autre cabinet. | `memoriserRepli (services/replis.ts) : la ligne est insérée par jointure sur app_user, le locataire n’est pas un paramètre` | Un appel forgé écrivait une ligne portant le locataire d’un AUTRE cabinet, et la lecture, qui ne filtrait que la personne, la relisait — une fuite d’un cabinet à l’autre sur un objet d’écran (revue hostile n°8, constat 3). | `REPLI-03` | Ne dit rien de la RLS : le rôle qui sert l’application la contourne encore (PLAN_RLS, étape 3 non exécutée). Ne borne pas ce qu’une personne range chez elle — c’est REPLI-04 qui le fait. |
| G-20 | PROUVÉE (2026-09-02) | Un exercice ou une mission ne se crée pas sur l’entité d’un autre cabinet. | `engagement.ts → creerExercice / creerMission (EngagementRuleError « isolation »)` | Le nom d’un client étranger dans nos papiers ; et l’inverse. | `isolation` | Ne remplace pas la RLS : l’application tourne sous un rôle qui la contourne (ADR-115), donc cette règle est la SEULE isolation à l’écriture pour ces deux objets. |
| G-21 | PROUVÉE (2026-09-02) | Une préférence de référentiel de seuil ne contourne pas la garde du résultat non représentatif. | `kernel/materiality.ts → proposeMateriality (motif « NOT applied »)` | Un seuil de 1 000 € — le plancher d’arrondi — sur une base négative. | `NOT applied` | Ne juge pas si 2 % du chiffre d’affaires est le bon critère de représentativité : c’est la règle du pack, écrite telle quelle. |
| G-24 | PROUVÉE (2026-09-03) | ETANCH-01/02/03 — aucun geste d’un cabinet ÉTRANGER n’entre dans un dossier : ni comme acteur, ni comme destinataire. | `core/membre.ts → assertMembre / assertDestinataire, appelées par sections, testing/grille, analytique, workpapers/lifecycle (les notes de revue vivent là, pas dans services/notes/), fsli, risk, programme, et 44 autres services depuis le câblage du 2026-09-03` | Quatre-vingt-seize fonctions de service exportées prennent un acteur ET écrivent ; à la découverte, QUATRE-VINGT-ONZE n’avaient aucune garde d’isolation. Sur treize gestes tentés depuis un autre cabinet, DIX ont été acceptés (le premier compte publié disait onze : faux, corrigé par la revue hostile n°9). Un cabinet étranger a notamment RÉÉCRIT le contenu d’un papier de travail, avec sa trace au journal. Le secret professionnel entre deux clients concurrents ne tenait qu’à `requireMember` sur les écrans. | `ETANCH-01` | Ne juge PAS le rôle (un membre est un membre : qui vise, qui clôt, qui scelle sont d’autres règles). Se tait pour un acteur nul (chemin SYSTÈME) et pour une mission dont l’équipe est VIDE (c’est l’acceptation qui parle). Ne couvre PAS encore 37 fonctions désignées par l’identifiant d’un objet FILS, écrites une par une avec leur raison dans core/couverture-etancheite.test.ts — le compte est publié et ne peut que baisser. Ne remplace PAS la RLS : le rôle servi la contourne encore (PLAN_RLS, étape 3 non exécutée). |
| G-25 | PROUVÉE (2026-09-03) | LOC-01/LOC-02 — sous un rôle qui n’a pas BYPASSRLS, une requête sans locataire posé LÈVE au lieu de rendre zéro ligne ; et une dérogation dont la clé n’est pas écrite est refusée. | `db/sans-locataire.ts → assertLocataire, appelé par q() ; la liste des chemins légitimes vit dans le même fichier` | Le silence lu comme un succès (règle 13) : écran vide sans message, tableau de bord à zéro obstacle, grille sans cellule — et rien au journal. Pire, les gardes SQL de 0037/0042/ADR-028 LISENT des tables pour décider de refuser : sans locataire elles ne voient rien, donc elles ne refusent rien. Un garde qui ne voit pas devient un garde qui laisse passer. | `LOC-01` | DÉSARMÉ tant que le rôle servi contourne la RLS — c’est le cas aujourd’hui, en production comme en local : il ne protège rien AUJOURD’HUI. Il regarde le contexte asynchrone, pas la base. Il ne couvre pas `db.query`/`db.exec` appelés directement (migrations, catalogue). |
| G-26 | PROUVÉE (2026-09-03) | BLOB-01 — une pièce ne se sert que si son CONTENU rend l’adresse par laquelle on l’a demandée. | `core/storage.ts → readBlob / verifierAdresse (le magasin est adressé par contenu : le chemin EST le sha256)` | Substitution de preuve dans un fichier d’audit, sans une ligne de journal. `saveBlob` fait `on conflict (storage_path) do nothing` : le PREMIER qui dépose un chemin le tient, et `readBlob` ne revérifiait rien. Sous otto_app (étape 3), un cabinet pouvait pré-insérer un couple chemin/octets et faire relire SES octets à la place de la facture d’un autre (revue hostile n°9, constat 9). | `BLOB-01` | Ne protège que la LECTURE d’une pièce donnée, jamais la CRÉATION : la politique blob_store_par_reference (0141) ferme la lecture croisée par engagement, mais garde `with check (true)` — sous otto_app, une insertion directe peut encore écrire pour un autre cabinet, fermé seulement à P2-05. Ne vérifie pas un chemin qui ne suit pas la convention `aa/sha256` (magasins d’avant 0028), et le dit. |

## Gardes déclarées — la preuve vit ailleurs, ou n’existe pas

| Code | Invariant | Tenu par | Rayon si elle tombe | Preuve | Où elle cesse de regarder |
|---|---|---|---|---|---|
| G-50 | Famille d’obstacles au visa « acceptation » : le dossier ne se vise pas tant qu’un obstacle de cette famille subsiste. | `obstacles.ts → obstaclesAuVisa, famille acceptation (motifs : obst.missionNonAcceptee)` | Un dossier visé avec un obstacle non levé de cette famille. | `obstacles.test.ts:41` | Le test nommé éprouve le motif ; le FAUX POSITIF (un dossier sain qui déclencherait la famille) n’a pas de fixture. |
| G-51 | Famille d’obstacles au visa « independance » : le dossier ne se vise pas tant qu’un obstacle de cette famille subsiste. | `obstacles.ts → obstaclesAuVisa, famille independance (motifs : obst.declarationNonSignee / obst.rotationDue)` | Un dossier visé avec un obstacle non levé de cette famille. | `team.test.ts` | Le test nommé éprouve le motif ; le FAUX POSITIF (un dossier sain qui déclencherait la famille) n’a pas de fixture. |
| G-52 | Famille d’obstacles au visa « reprise » : le dossier ne se vise pas tant qu’un obstacle de cette famille subsiste. | `obstacles.ts → obstaclesAuVisa, famille reprise (motifs : obst.repriseNonStatuee)` | Un dossier visé avec un obstacle non levé de cette famille. | `carryforward.test.ts` | Le test nommé éprouve le motif ; le FAUX POSITIF (un dossier sain qui déclencherait la famille) n’a pas de fixture. |
| G-53 | Famille d’obstacles au visa « questionnaire » : le dossier ne se vise pas tant qu’un obstacle de cette famille subsiste. | `obstacles.ts → obstaclesAuVisa, famille questionnaire (motifs : obst.facteursNonStatues / obst.ouiSansPrecision / obst.questionsSectionSansReponse)` | Un dossier visé avec un obstacle non levé de cette famille. | `services/questionnaire.test.ts` | Le test nommé éprouve le motif ; le FAUX POSITIF (un dossier sain qui déclencherait la famille) n’a pas de fixture. |
| G-54 | Famille d’obstacles au visa « processus » : le dossier ne se vise pas tant qu’un obstacle de cette famille subsiste. | `obstacles.ts → obstaclesAuVisa, famille processus (motifs : obst.processusChangementsNonStatues / obst.entretienEcartsCandidats)` | Un dossier visé avec un obstacle non levé de cette famille. | `processus.test.ts, entretiens.test.ts` | Le test nommé éprouve le motif ; le FAUX POSITIF (un dossier sain qui déclencherait la famille) n’a pas de fixture. |
| G-55 | Famille d’obstacles au visa « programme » : le dossier ne se vise pas tant qu’un obstacle de cette famille subsiste. | `obstacles.ts → obstaclesAuVisa, famille programme (motifs : obst.posteSansProcedure)` | Un dossier visé avec un obstacle non levé de cette famille. | `obstacles.test.ts:93` | Le test nommé éprouve le motif ; le FAUX POSITIF (un dossier sain qui déclencherait la famille) n’a pas de fixture. |
| G-56 | Famille d’obstacles au visa « boucle » : le dossier ne se vise pas tant qu’un obstacle de cette famille subsiste. | `obstacles.ts → obstaclesAuVisa, famille boucle (motifs : obstacles de loop.ts (b.obstacles[].cle))` | Un dossier visé avec un obstacle non levé de cette famille. | `loop.test.ts` | Le test nommé éprouve le motif ; le FAUX POSITIF (un dossier sain qui déclencherait la famille) n’a pas de fixture. |
| G-57 | Famille d’obstacles au visa « pointage » : le dossier ne se vise pas tant qu’un obstacle de cette famille subsiste. | `obstacles.ts → obstaclesAuVisa, famille pointage (motifs : obst.pointageEcart)` | Un dossier visé avec un obstacle non levé de cette famille. | `tieout.test.ts` | Le test nommé éprouve le motif ; le FAUX POSITIF (un dossier sain qui déclencherait la famille) n’a pas de fixture. |
| G-58 | Famille d’obstacles au visa « tirage » : le dossier ne se vise pas tant qu’un obstacle de cette famille subsiste. | `obstacles.ts → obstaclesAuVisa, famille tirage (motifs : obst.ligneSortieDuTirage)` | Un dossier visé avec un obstacle non levé de cette famille. | `services/retirage.test.ts` | Le test nommé éprouve le motif ; le FAUX POSITIF (un dossier sain qui déclencherait la famille) n’a pas de fixture. |
| G-59 | Famille d’obstacles au visa « evaluation » : le dossier ne se vise pas tant qu’un obstacle de cette famille subsiste. | `obstacles.ts → obstaclesAuVisa, famille evaluation (motifs : obst.evaluation)` | Un dossier visé avec un obstacle non levé de cette famille. | `tests/parcours.test.ts:186` | Le test nommé éprouve le motif ; le FAUX POSITIF (un dossier sain qui déclencherait la famille) n’a pas de fixture. |
| G-60 | Famille d’obstacles au visa « achevement » : le dossier ne se vise pas tant qu’un obstacle de cette famille subsiste. | `obstacles.ts → obstaclesAuVisa, famille achevement (motifs : obst.achevementNonConclu)` | Un dossier visé avec un obstacle non levé de cette famille. | `completion.test.ts` | Le test nommé éprouve le motif ; le FAUX POSITIF (un dossier sain qui déclencherait la famille) n’a pas de fixture. |
| G-61 | Famille d’obstacles au visa « circularisation » : le dossier ne se vise pas tant qu’un obstacle de cette famille subsiste. | `obstacles.ts → obstaclesAuVisa, famille circularisation (motifs : obst.circEcartNonExplique)` | Un dossier visé avec un obstacle non levé de cette famille. | `circularisations.test.ts` | Le test nommé éprouve le motif ; le FAUX POSITIF (un dossier sain qui déclencherait la famille) n’a pas de fixture. |
| G-62 | Famille d’obstacles au visa « ipe » : le dossier ne se vise pas tant qu’un obstacle de cette famille subsiste. | `obstacles.ts → obstaclesAuVisa, famille ipe (motifs : obst.ipeQuestionNonPosee)` | Un dossier visé avec un obstacle non levé de cette famille. | `ipe.test.ts` | Le test nommé éprouve le motif ; le FAUX POSITIF (un dossier sain qui déclencherait la famille) n’a pas de fixture. |
| G-63 | Famille d’obstacles au visa « jalons » : le dossier ne se vise pas tant qu’un obstacle de cette famille subsiste. | `obstacles.ts → obstaclesAuVisa, famille jalons (motifs : obst.jalonEnRetard)` | Un dossier visé avec un obstacle non levé de cette famille. | **aucune** | Aucun test ne nomme ce motif : la famille est déclarée, pas éprouvée. Reporté. |
| G-64 | Famille d’obstacles au visa « materialite » : le dossier ne se vise pas tant qu’un obstacle de cette famille subsiste. | `obstacles.ts → obstaclesAuVisa, famille materialite (motifs : obst.basculeSansSection / obst.basculeSansDemandeCtt)` | Un dossier visé avec un obstacle non levé de cette famille. | `services/obstacles-materialite.test.ts` | Le test nommé éprouve le motif ; le FAUX POSITIF (un dossier sain qui déclencherait la famille) n’a pas de fixture. |
| G-65 | Famille d’obstacles au visa « iaNonValide » : le dossier ne se vise pas tant qu’un obstacle de cette famille subsiste. | `obstacles.ts → obstaclesAuVisa, famille iaNonValide (motifs : obst.iaNonValide (NOTIF-01))` | Un dossier visé avec un obstacle non levé de cette famille. | `services/notifications.test.ts` | Le test nommé éprouve le motif ; le FAUX POSITIF (un dossier sain qui déclencherait la famille) n’a pas de fixture. |
| G-66 | Famille d’obstacles au visa « analytique » : le dossier ne se vise pas tant qu’un obstacle de cette famille subsiste. | `obstacles.ts → obstaclesAuVisa, famille analytique (motifs : obst.analytiquePerimee (ANA-04))` | Un dossier visé avec un obstacle non levé de cette famille. | `services/obstacles-analytique.test.ts` | Le test nommé éprouve le motif ; le FAUX POSITIF (un dossier sain qui déclencherait la famille) n’a pas de fixture. |
| G-67 | Famille d’obstacles au visa « demandes » : le dossier ne se vise pas tant qu’un obstacle de cette famille subsiste. | `obstacles.ts → obstaclesAuVisa, famille demandes (motifs : obst.demandeEnRetard (H-1))` | Un dossier visé avec un obstacle non levé de cette famille. | `services/obstacles-demandes.test.ts` | Le test nommé éprouve le motif ; le FAUX POSITIF (un dossier sain qui déclencherait la famille) n’a pas de fixture. |
| G-68 | Famille d’obstacles au visa « unsupported_sample_items — avertissement, drapeau nep-fr à off » : le dossier ne se vise pas tant qu’un obstacle de cette famille subsiste. | `obstacles.ts → obstaclesAuVisa, famille unsupported_sample_items — avertissement, drapeau nep-fr à off (motifs : obst.lignesNonConclues / obst.lignesConclusionPerimee)` | Un dossier visé avec un obstacle non levé de cette famille. | `testing/grille.test.ts` | Le test nommé éprouve le motif ; le FAUX POSITIF (un dossier sain qui déclencherait la famille) n’a pas de fixture. |
| G-70 | « RAS » n’explique pas un écart de circularisation. | `circularisations.ts (CircularisationError « RAS »)` | Une explication vide qui passe pour une explication. | `circularisations.test.ts` | Ne reconnaît que la lettre « RAS » et le vide ; « rien à signaler » en toutes lettres passe. |
| G-71 | Le tirage est déterministe par graine : même population, même graine, même sélection. | `kernel/sampling.ts (attributeDraw, monetary draw)` | Une sélection non rejouable : le papier ne prouve plus quel élément a été tiré. | `kernel.test.ts` | Ne fige pas les sélections du monde de démonstration en fichier d’or — reporté. |
